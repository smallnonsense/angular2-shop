import { ProductCartPipe } from './product-cart.pipe';

describe('ProductCartPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductCartPipe();
    expect(pipe).toBeTruthy();
  });
});
